pharo/
.pharo.pid
.pharo.log
PharoDebug.log
.tmp/
*.image
*.changes

# Local-only files (machine-specific, per-user)
*.local
*.local.*

# Beads daemon/database state (issues.jsonl + interactions.jsonl are tracked)
.beads/beads.db*
.beads/daemon-*
.beads/daemon.log
.beads/.local_version

# Quarry session-transcript captures (pkit-kcps)
.punt-labs/quarry/captures/

# Claude Code local settings (per-machine, may contain paths/tokens)
.claude/settings.local.json
